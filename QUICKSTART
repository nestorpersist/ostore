1. Make sure the data directory is empty
   (unless you are testing with a 
    preexisting database).

2. Use sbt 0.11.3

3. Start sever

	cd core
	sbt
	run

4. Start ui

	cd ui
	sbt
	container:start

5. In browser goto url

	http://127.0.0.1:8080/?restartApplication

6. Add a database

	name: test
	file: config/test.db

7. Add values to a table

	select the test database
	select tables
	select tab1
	upload
	file: tab1.data

8. To quit

	for database test

	stop

	Shutdown both core and ui


